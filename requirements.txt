# solaredge_modbus 0.8.0 nutzt noch die alte pymodbus-API (Endian aus
# pymodbus.constants). pymodbus >= 3.7 hat diese entfernt, daher hier fest
# auf eine kompatible Version gepinnt.
pymodbus==3.6.9
solaredge_modbus==0.8.0
requests==2.32.3
